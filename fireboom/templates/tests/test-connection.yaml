apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fireboom.fullname" . }}-test-connection"
  labels:
    {{- include "fireboom.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fireboom.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
